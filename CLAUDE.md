# mcplibrarian Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-02-21

## Active Technologies

- Python 3.10+ (existing; all new modules extend `src/mcp_dockerize/`) + Click 8.1+ (CLI), Jinja2 3.1+ (templates), docker 7.0+ (Python SDK), python-dotenv 1.0+ (env file parsing), toml 0.10.2+ (pyproject.toml parsing) — all already in pyproject.toml (001-mcp-librarian)

## Project Structure

```text
src/
tests/
```

## Commands

cd src [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] pytest [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] ruff check .

## Code Style

Python 3.10+ (existing; all new modules extend `src/mcp_dockerize/`): Follow standard conventions

## Recent Changes

- 001-mcp-librarian: Added Python 3.10+ (existing; all new modules extend `src/mcp_dockerize/`) + Click 8.1+ (CLI), Jinja2 3.1+ (templates), docker 7.0+ (Python SDK), python-dotenv 1.0+ (env file parsing), toml 0.10.2+ (pyproject.toml parsing) — all already in pyproject.toml

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
