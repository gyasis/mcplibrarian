# Docker Compose configuration for tableau-mcp-server
# Generated by mcplibrarian

version: '3.8'

services:
  tableau-mcp-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mcp-tableau-mcp-server
    stdin_open: true  # Required for MCP stdio
    tty: false
    volumes:
      - type: bind
        source: /home/gyasis/Documents/code/tableau-mcp
        target: /app
        read_only: true
    env_file:
      - /home/gyasis/Documents/code/tableau-mcp/.env

# ⚠️  SECURITY WARNINGS:
#    - Potential hardcoded credential in server.py. Use environment variables instead.
