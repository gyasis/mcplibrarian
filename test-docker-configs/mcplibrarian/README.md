# Docker Configuration for mcplibrarian

Auto-generated by `mcplibrarian`

## Server Information

- **Type**: python-uv
- **Python Version**: 3.10
- **Entry Point**: mcplibrarian
- **Deployment Pattern**: self-contained

## Quick Start

### Build Image

```bash
docker compose build
```

### Run Container

```bash
docker compose up -d
```

### Test MCP Connection

```bash
echo '{"jsonrpc":"2.0","method":"initialize","params":{}}' | docker compose run --rm mcplibrarian
```

## Deployment Pattern: Self-Contained

This server uses **self-contained** deployment:
- Code is copied into the container
- All dependencies installed with uv
- Portable and reproducible

**Advantages:**
- Fully isolated from host
- Can run anywhere
- Reproducible builds


## Claude Code Integration

Add to `~/.config/Claude/claude_desktop_config.json`:

```json
{
  "mcpServers": {
    "mcplibrarian": {
      "command": "docker",
      "args": ["compose", "-f", "test-docker-configs/mcplibrarian/docker-compose.yml", "run", "--rm", "mcplibrarian"]
    }
  }
}
```

## Cleanup

```bash
docker compose down
docker image rm mcp-mcplibrarian
```

---

*Generated by mcplibrarian | Non-destructive, read-only analysis*
