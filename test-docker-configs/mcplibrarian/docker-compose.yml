# Docker Compose configuration for mcplibrarian
# Generated by mcplibrarian

version: '3.8'

services:
  mcplibrarian:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mcp-mcplibrarian
    stdin_open: true  # Required for MCP stdio
    tty: false
