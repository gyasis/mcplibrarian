
    # Auto-generated routing rules for playwright-mcp-wrapper
    if server_name == "playwright-mcp-wrapper":


        # Default fallback
        else:
            tool_name = "unknown"
            params = {"query": query}
